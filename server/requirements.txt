# ACW Server — REST + SSE API for AWS Coworker
# Wraps the CLI via Claude Code SDK

# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
sse-starlette>=2.1.0
python-multipart>=0.0.12

# Claude Agent SDK
claude-agent-sdk>=0.1.0

# YAML parsing (for markdown frontmatter)
pyyaml>=6.0.2

# Diagram generation
diagrams>=0.24.0

# HTTP client (for REPL test client)
httpx>=0.27.0

# Utilities
python-dotenv>=1.0.1
